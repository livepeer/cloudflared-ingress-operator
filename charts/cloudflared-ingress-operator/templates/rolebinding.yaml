apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "cloudflared-ingress-operator.fullname" . }}-configmap
  namespace: {{ .Values.cloudflared.namespace }}
  labels:
    {{- include "cloudflared-ingress-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "cloudflared-ingress-operator.fullname" . }}-configmap
subjects:
  - kind: ServiceAccount
    name: {{ include "cloudflared-ingress-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
